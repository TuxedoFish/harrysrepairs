build:
  config:
    REACT_APP_BACKEND_URL: https://harrysrepairs-staging-strapi.herokuapp.com
    NODE_ENV: ${NODE_ENV}
    GOOGLE_ANALYTICS_ID: ${GOOGLE_ANALYTICS_ID}
  docker:
    web: web/Dockerfile
run:
  web: npm run start:prod